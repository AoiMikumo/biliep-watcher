# Runtime data — collected snapshots, regenerated by the watcher. Not part of the tool.
data/

# Local-only legacy migration helper (a fresh deployment never needs it).
migrate.js

# OS / editor cruft
.DS_Store
Thumbs.db
*.log
node_modules/
